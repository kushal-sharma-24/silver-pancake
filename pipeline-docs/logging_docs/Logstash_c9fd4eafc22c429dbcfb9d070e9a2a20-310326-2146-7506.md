## **Logstash** 

Logstash is an open-source data collection engine with real-time pipelining capabilities. Logstash can dynamically unify data from disparate sources and normalize the data into destinations of your choice. Cleanse and democratize all your data for diverse advanced downstream analytics and visualization use cases. 

While Logstash originally drove innovation in log collection, its capabilities extend well beyond that use case. Any type of event can be enriched and transformed with a broad array of input, filter, and output plugins, with many native codecs further simplifying the ingestion process. Logstash accelerates your insights by harnessing a greater volume and variety of data. 


![](logging_docs/markdown_converted/images/Logstash_c9fd4eafc22c429dbcfb9d070e9a2a20-310326-2146-7506.pdf-0001-03.png)


**----- Start of picture text -----**<br>
FIPS<br>Not FIPS-compliant<br>**----- End of picture text -----**<br>


**The ingestion workhorse for Elasticsearch and more.** Horiz ontally scalable data processing pipeline with strong Elasticsearch and Kibana synergy **Pluggable pipeline architecture.** Mix, match, and orchestrate different inputs, filters, and outputs to play in pipeline harmony **Community-extensible and developer-friendly plugin ecosystem.** Over 200 plugins available, plus the flexibility of creating and contributing your own 

## **Notes** 

Documentation Docker image 

## **Supported versions** 


![](logging_docs/markdown_converted/images/Logstash_c9fd4eafc22c429dbcfb9d070e9a2a20-310326-2146-7506.pdf-0001-08.png)


**----- Start of picture text -----**<br>
Env version<br>Colo 5.6<br>ATM 6.8 or 7.x<br>**----- End of picture text -----**<br>


## **Shipper extra fields** 

## Installation 

- [Getting started][Setting Up and Running] 1.  Download and unzip Heartbeat or use docker image or get it from artifactory 2.  Edit the metricbeat.yml configuration file 3.  Edit your pipeline.yml pipeline file 4.  Start Logstash 

## Configuration 

logstash.yml Logstash plugins 

## Useful filter plugins 

## Simple Logstash config 

grok - parsing string json - decoding json like string A simple configuration for Logsatsh with a single pipeline mutate - basic add/remove/change/rename operations prune - field white/blacklisting ruby - running ruby code for custom operations `config:` truncate - shortening a long text `reload: automatic: false http: host: 0.0.0.0 port: 9600` Env certificate `log: level: info` Download it properly - Internal Certificate Automation API (for `node:` Non-ATM environments) `name: localhost` In **ATM** , it should be a part of the Golden image OR can be downloaded by following the steps mentioned here by DISCO `xpack.monitoring.enabled: false` Team Use a hack 

A simple configuration for Logsatsh with a single pipeline 

## Simple Logstash pipeline 

```
openssl s_client -connect
<FILL_ENV_ENDPOINT>:443 -showcerts
```

A simple pipeline, tracking a file and parsing its content... 

```
input {
  file {
    path => "/tmp/sample.txt"
    start_position => "end"
  }
}
filter {
  grok {
           match => { "message" => "%
{TIMESTAMP_ISO8601:@timestamp} %{LOGLEVEL:level}%
{GREEDYDATA:description}" }
    remove_field => [ "message" ]
  }
  mutate {
    replace => {
      "correlation_id" => "logstash-file-https"
      "roletype" => "lgs"
      "application" => "my-super-app"
    }
  }
}
output {
  http {
    url => "https://<FILL_ENV_ENDPOINT>:443"
    http_method => "put"
        cacert => "<PATH_TO_ENV_CERT>"
  }
}
```

Copy the certificated which has the same issuer as subject 

## Lumberjack output protocol 

This is a less recommended and reliable way 

```
output {
  lumberjack {
    hosts           => "<FILL_ENV_ENDPOINT>"
    port            => 5044
    ssl_certificate => "<PATH_TO_ENV_CERT>"
    codec           => "json"
  }
}
```

## FAQ 

If you have any questions, hit us via slack on #ask-logging. 


## **WinlogBeat** 

WinlogBeat ships Windows event logs to Elasticsearch or Logstash. You can install it as a Windows service. 

WinlogBeat reads from one or more event logs using Windows APIs, filters the events based on user-configured criteria, then sends the event data to the configured outputs (Elasticsearch or Logstash). WinlogBeat watches the event logs so that new event data is sent in a timely manner. The read position for each event log is persisted to disk to allow WinlogBeat to resume after restarts. 


![](logging_docs/markdown_converted/images/WinlogBeat_1ce762abb1b34cfcab3a26241d36b3cd-310326-2145-7496.pdf-0001-03.png)


## **FIPS** 

Windows package is not FIPS-compliant, but it has permanent exception. 

WinlogBeat can capture event data from any event logs running on your system. For example, you can capture events such as: 

application events hardware events security events system events 

## **Notes** 

## **Supported versions** 

## **Shipper extra fields** 

|Documentation|**Env**|**version**|
|---|---|---|
||Colo|5.6|
||ATM|6.8 or 7.x|



## Installation 

- [Quick start] 1.  On Windows: a.  install package: install package from Artifactory - ext-winlogbeat. The `ext` denotes that Travel team grabbed the vendor choco package, found that it had direct calls to an external URL, and repackaged a version with the actual installer inside so there is no network traffic to anything but Artifactory. They do that in the repo psp/chocolatey-external in the elastic folder. They include YAML files in the tools folder which will set up the service correctly, based on a few environment variables being present. Environment variables it looks for: `CNQR-TRAVEL-ROLETYPE` , `CNQR-TRAVEL-LOGSTASH` (an URL), `CNQRTRAVEL-LOGSTASH_SSL` (by default `true` ). or by Ansible, as in the monolith. directly from PowerShell: 

```
# Example 1 (simple, assumes you have env vars set, and chococlatey source set)
choco upgrade ext-winlogbeat -y
# Example 2
[System.Environment]::SetEnvironmentVariable("CNQR-TRAVEL-LogStash", 'lp-index-sea.
concurasp.com', 'Machine')
[System.Environment]::SetEnvironmentVariable("CNQR-TRAVEL-LogStash", 'lp-index-sea.
concurasp.com', 'Process')
choco upgrade ext-winlogbeat -y -s https://artifactory.concurtech.net/artifactory/api
/nuget/choco-release
# Example 3
Write-Host"*** Starting logstash_update ***"
Write-Host"Artifactory_URL: $artifactory_url"
Write-Host"Chocolatey Repo: $choco_source"
Write-Host"LogStash Server: $log_stash"
"Adding Release and Sandbox sources in chocolatey with precedence to the release repo"| W
rite-Warning
c:\ProgramData\chocolatey\choco.exe source add -n=Release -s="${artifactory_url}/api
/nuget/${choco_source}"--priority=10
c:\ProgramData\chocolatey\choco.exe source add -n=Sandbox -s="https://artifactory.
concurtech.net/artifactory/api/nuget/choco-sandbox"--priority=20
Write-Host"Setting Logstash URL"
[System.Environment]::SetEnvironmentVariable("CNQR-TRAVEL-LogStash", $log_stash, 'Machine'
)
[System.Environment]::SetEnvironmentVariable("CNQR-TRAVEL-LogStash", $log_stash, 'Process'
)
# A chocolatey package failure should not cause other packages to never install
@(
    {c:\ProgramData\chocolatey\choco.exe upgrade ext-winlogbeat -y }
) | ForEach-Object{
    try {
        "Executing expression: $($_.ToString())"| Write-Warning
        Invoke-Command-ScriptBlock$_ -ErrorActionContinue
    }
    catch {
        $_.Exception.Message | Write-Warning
    }
}
```

## Configuration 

winlogbeat.reference.yml config options 

## Additional fields 

## Simple config sample 

In case you need to add some additional fields to your message which aren't present in the original message, you can achieve this like that: 

```
fields_under_root: true
fields:
  extra_fields:
          field_a: value_a
          field_b: value_b
```

```
winlogbeat:
  registry_file: C:/ProgramData/winlogbeat/.
winlogbeat.yml
  event_logs:
    - name: Application
      ignore_older: 24h
    - name: System
output:
  logstash:
    hosts: <FILL_ENV_ENDPOINT>:10301
    ssl:
       enabled: true
       verification_mode: none
logging:
  to_files: true
  files:
    path: C:/ProgramData/winlogbeat/Logs
    rotateeverybytes: 10485760 # = 10MB
    keepfiles: 7
  level: info
```

## FAQ 

If you have any question, hit us via slack on #ask-logging. 

_Can i run it in a Linux machine?_ 

No. 


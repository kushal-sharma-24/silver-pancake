## **HeartBeat** 

HeartBeat is a lightweight daemon that you install on a remote server to periodically check the status of your services and determine whether they are available. Unlike MetricBeat, which only tells you if your servers are up or down, HeartBeat tells you whether your services are reachable. 

HeartBeat is useful when you need to verify that you’re meeting your service level agreements for service uptime. It’s also useful for other scenarios, such as security use cases, when you need to verify that no one from the outside can access services on your private enterprise server. 


![](logging_docs/markdown_converted/images/HeartBeat_31c2b816b21d4a9fac30287f30e7889d-310326-2145-7498.pdf-0001-03.png)


## **FIPS** 

Linux requires package `elastic-heartbeat-fips` , which is FIPS-compliant. Windows package is not FIPS-compliant, but it has permanent exception. 

You can configure HeartBeat to ping all DNS-resolvable IP addresses for a specified hostname. That way, you can check all services that are load-balanced to see if they are available. 

When you configure HeartBeat, you specify monitors that identify the hostnames that you want to check. Each monitor runs based on the schedule that you specify. For example, you can configure one monitor to run every 10 minutes, and a different monitor to run between the hours of 9:00 and 17:00. 

HeartBeat currently supports monitors for checking hosts via: 

- ICMP (v4 and v6) Echo Requests. Use the `icmp` monitor when you simply want to check whether a service is available. This monitor requires root access. 

- TCP. Use the `tcp` monitor to connect via TCP. You can optionally configure this monitor to verify the endpoint by sending and/or receiving a custom payload. 

- HTTP. Use the `http` monitor to connect via HTTP. You can optionally configure this monitor to verify that the service returns the expected response, such as a specific status code, response header, or content. 

The `tcp` and `http` monitors both support SSL/TLS and some proxy settings. 

## **Notes** 

## **Supported versions** 

## **Shipper extra fields** 

|Documentation<br>Docker image<br>**Env**|**version**<br>Since there were problem merging mapping of<br>different beat major versions, each has its own|
|---|---|
|Colo|5.6<br>index pattern and cannot be viewed together.|
|ATM|6.8 or 7.x<br>-<br>`@timestamp`<br>The timestamp when<br>the event log record was generated.|
||/<br>/<br>Beat fields<br>6.8<br>5.6|
||host info in bucket<br>or<br>`agent`|
||`beat`|
||/<br>/<br>Common monitoring fields<br>6.8<br>5.6|
||Fields of our monitors|



## Installation 

[Quick start] 

1.  On Linux: a.  install package: install Concur package `elasic-heartbeat-fips` from the Artifactory repo `ext-yum-selfserve-local-v2/centos /7/x86_64` or install vendor package `elasic-heartbeat` from the Artifactory repo `ext-yum-selfserve-local-v2/centos /7/x86_64` _(not FIPS-compliant!)_ or download and install vendor package _(not FIPS-compliant!)_ b.  edit the configuration file `heartbeat.yml` c.  start service 2.  Run in Docker container _(not FIPS-compliant!)_ 

## Configuration 

heartbeat.reference.yml heartbeat monitors 

## Simple config sample 

A simple configuration to check local port 443 via tcp every 5 seconds... 

```
heartbeat:
  monitors:
  - type: tcp
    schedule: "@every 5s"
    hosts: ["localhost"]
    ports: [443]
output:
  logstash:
    hosts: <FILL_ENV_ENDPOINT>:10301
    ssl:
       enabled: true
       verification_mode: none
```

```
logging:
  to_files: true
  files:
    path: /var/log/heartbeat
    rotateeverybytes: 10485760 # = 10MB
    keepfiles: 7
  level: info
```

## Additional fields 

In case you need to add some additional fields to your message which aren't present in the original message, you can achieve this like that: 

```
fields_under_root: true
fields:
  extra_fields:
          field_a: value_a
          field_b: value_b
```

## FIPS compliance 

There is a small change needed to be FIPS compliant 

```
output:
  logstash:
    hosts: <FILL_ENV_ENDPOINT>:10301
    ssl:
       enabled: true
       supported_protocols: [TLSv1.2]
       verification_mode: full
       certificate_authorities:
       - <PATH_TO_ENV_CERT>
```

## FAQ 

If you have any question, hit us via slack on #ask-logging. 

